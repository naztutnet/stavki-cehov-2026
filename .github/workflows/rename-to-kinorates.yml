name: KinoRates migration archive

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Migration status
        run: echo "KinoRates migration is complete. No action is required."
